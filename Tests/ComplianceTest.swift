import XCTest

class ComplianceTest: XCTestCase {
    
    
    func testKoaraToHtml() {
        var kd = try String(contentsOf: URL(#file), encoding: String.Encoding.utf8)
    
        
        //kd = File.read("#{TEST_DIR}/input/#{folder}/#{testcase}.kd")
        
    }

    
}
